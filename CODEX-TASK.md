# CODEX-TASK

## 目标
把现有演示页改造成可接通的真实提交系统：先实现可独立完成的服务端与接口逻辑，再把账号配置留给你自己补齐。

## 工作顺序（按可独立交付优先）

1. 已完成：把前端提交行为改为真实 API 请求（按钮与表单保留不重做）
   - 文件：`smart-rfq-visual-demo.html`
2. 已完成：新增服务端接口 `/api/rfq`，校验并落库
   - 文件：`server.js`
3. 已完成：新增邮件通知逻辑（先保存记录，再发通知，失败不丢记录）
   - 文件：`server.js`
4. 已完成：补齐后端配置模板，并明确“尚未连接”文案
   - 文件：`.env.example`
5. 已完成：新增可执行的端到端接口测试
   - 文件：`tests/rfq-api.test.js`
   - 脚本：`npm test`

## 剩余你需要配置（你的账号）

- 配置邮件服务（SMTP）账号信息到 `.env` 或系统环境变量
  - `MAIL_HOST`
  - `MAIL_PORT`
  - `MAIL_SECURE`
  - `MAIL_USER`
  - `MAIL_PASS`
  - `MAIL_FROM`
  - `MAIL_TO`

## 注意

- 当前已明确标注“尚未连接”（未配置邮件参数时），不会用模拟成功替代真实执行。
- 当前无“访客读取他人询价”接口，提交端点只接收新增，不提供列表查询。
